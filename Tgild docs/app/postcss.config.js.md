# `app/postcss.config.js` — PostCSS Config

- **Purpose**: PostCSS configuration (legacy — Tailwind v4 uses Vite plugin instead)
- **Content**: Empty/default — likely unused since Tailwind v4 is configured via Vite plugin in `vite.config.ts`
