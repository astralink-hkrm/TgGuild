# `app/src/contexts/DropZoneContext.tsx` — Drop Zone (Stub)

- **Purpose**: Minimal placeholder context for drag-drop state
- **Exports**: `DropZoneProvider`, `useDropZone`
- **Current state**: Initial implementation only — returns `{ isDragging: false, handleDragOver: () => {}, handleDragLeave: () => {}, handleDrop: () => {} }`
- **Note**: Drag-drop handling is done via `ExternalDropBlocker` and direct DOM event listeners instead
