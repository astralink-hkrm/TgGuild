# `app/src/hooks/useFileDrop.ts` — File Drop (Stub)

- **Purpose**: Stub hook for drag-drop file handling
- **Exports**: `useFileDrop(folderId)`
- **Current state**: Returns empty handler — native drag-drop is disabled via `ExternalDropBlocker`, and file upload is done via dialog instead
