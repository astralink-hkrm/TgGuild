# `app/src/components/ThemeToggle.tsx` — Theme Switcher

- **Purpose**: Dark/light theme toggle button
- **Props**: `className?`
- **Behavior**: Reads/writes theme via `useTheme()` from ThemeContext
- **Icon**: Sun/Moon toggle via lucide-react
- **Style**: Pill-shaped button with glass effect and smooth icon transition
