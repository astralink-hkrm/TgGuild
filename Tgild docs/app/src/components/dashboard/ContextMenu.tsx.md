# `app/src/components/dashboard/ContextMenu.tsx` — Right-Click Menu

- **Purpose**: Context menu on file right-click
- **Props**: `x`, `y`, `item`, `onClose`, action callbacks
- **Items**: Download, Delete, Rename, Open, Share, Move to
- **Style**: `.context-menu` CSS — glass effect, drop shadow, rounded corners, Framer Motion entry animations
- **Behavior**: Closes on click outside or on action
